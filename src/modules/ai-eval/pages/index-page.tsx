import {
  createModuleIndexMetadata,
  createModuleIndexPage,
} from '@/core/content/index-page'

export default createModuleIndexPage('ai-eval')
export const generateMetadata = createModuleIndexMetadata('ai-eval')
